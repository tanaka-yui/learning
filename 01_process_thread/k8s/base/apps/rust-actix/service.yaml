apiVersion: v1
kind: Service
metadata:
  name: rust-actix-server
  namespace: process-thread
spec:
  type: ClusterIP
  selector:
    app: rust-actix-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
