apiVersion: v1
kind: Service
metadata:
  name: php-server
  namespace: process-thread
spec:
  type: ClusterIP
  selector:
    app: php-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
