apiVersion: v1
kind: Service
metadata:
  name: ruby-server
  namespace: process-thread
spec:
  type: ClusterIP
  selector:
    app: ruby-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
