apiVersion: apps/v1
kind: Deployment
metadata:
  name: rust-axum-server
  namespace: process-thread
  labels:
    app: rust-axum-server
    language: rust-axum
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rust-axum-server
  template:
    metadata:
      labels:
        app: rust-axum-server
        language: rust-axum
    spec:
      containers:
        - name: rust-axum-server
          image: pt-rust-axum:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: HEAVY_CALC_N
              value: "43"
            - name: TOKIO_WORKERS
              value: "1"
          resources:
            requests:
              cpu: "1"
              memory: "64Mi"
            limits:
              cpu: "1"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
