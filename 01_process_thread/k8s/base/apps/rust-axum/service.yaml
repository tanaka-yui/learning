apiVersion: v1
kind: Service
metadata:
  name: rust-axum-server
  namespace: process-thread
spec:
  type: ClusterIP
  selector:
    app: rust-axum-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
