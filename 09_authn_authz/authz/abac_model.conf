# ABAC モデル定義
# サブジェクトの属性(ここでは r.sub そのもの)とリソース所有者を比較する。
# 所有者 == 実行者 なら許可。リソース属性は Go 側の struct で渡す。

[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.sub == r.obj.Owner && r.act == "edit"
