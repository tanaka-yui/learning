services:
  sqli-vulnerable-backend:
    build: ./sql-injection/vulnerable/backend
    ports:
      - "8080:8080"
    profiles:
      - sql-injection

  sqli-secure-backend:
    build: ./sql-injection/secure/backend
    ports:
      - "8081:8080"
    profiles:
      - sql-injection
