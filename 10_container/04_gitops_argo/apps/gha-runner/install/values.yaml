# actions-runner-controller-charts/gha-runner-scale-set-controller values
# image.tag is intentionally omitted — the chart default matches the chart version (0.14.2+).
# Pinning tag: 0.9.3 here would conflict with chart 0.14.2 flags (-runner-max-concurrent-reconciles).
replicaCount: 1
image:
  repository: ghcr.io/actions/gha-runner-scale-set-controller
