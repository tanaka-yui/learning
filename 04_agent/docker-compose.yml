services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=mastra
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5
    profiles: [mastra, mastra-fastify]

  mastra:
    build: ./mastra/backend
    ports:
      - "4001:4001"
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_PROFILE=${AWS_PROFILE:-default}
      - AWS_PROVIDER=${AWS_PROVIDER:-sso}
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mastra
      - WORKSPACE_BASE_PATH=/app
    volumes:
      - ${HOME}/.aws:/root/.aws:ro
      - ./mastra/backend:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    profiles: [mastra]

  mastra-fastify:
    build: ./mastra-fastify/backend
    ports:
      - "4002:4002"
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_PROFILE=${AWS_PROFILE:-default}
      - AWS_PROVIDER=${AWS_PROVIDER:-sso}
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mastra
      - WORKSPACE_BASE_PATH=/app
    volumes:
      - ${HOME}/.aws:/root/.aws:ro
      - ./mastra-fastify/backend:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    profiles: [mastra-fastify]

  strands-python:
    build: ./strands-python/backend
    ports:
      - "4003:4003"
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_PROFILE=${AWS_PROFILE:-default}
    volumes:
      - ${HOME}/.aws:/root/.aws:ro
      - ./strands-python/backend:/app
      - /app/.venv
    profiles: [strands-python]

  strands-typescript:
    build: ./strands-typescript/backend
    ports:
      - "4004:4004"
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_PROFILE=${AWS_PROFILE:-default}
    volumes:
      - ${HOME}/.aws:/root/.aws:ro
      - ./strands-typescript/backend:/app
      - /app/node_modules
    profiles: [strands-typescript]

  claude-agent-sdk:
    build: ./claude-agent-sdk/backend
    ports:
      - "4005:4005"
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_PROFILE=${AWS_PROFILE:-default}
    volumes:
      - ${HOME}/.aws:/root/.aws:ro
      - ./claude-agent-sdk/backend:/app
      - /app/node_modules
    profiles: [claude-agent-sdk]

  mastra-frontend:
    build: ./mastra/frontend
    ports:
      - "5001:5001"
    environment:
      - VITE_BACKEND_URL=http://localhost:4001
    volumes:
      - ./mastra/frontend:/app
      - /app/node_modules
    depends_on:
      - mastra
    profiles: [mastra]

  mastra-fastify-frontend:
    build: ./mastra-fastify/frontend
    ports:
      - "5002:5002"
    environment:
      - VITE_BACKEND_URL=http://localhost:4002
      - VITE_THEME_COLOR=#8b5cf6
      - VITE_FRAMEWORK_NAME=mastra-fastify
    volumes:
      - ./mastra-fastify/frontend:/app
      - /app/node_modules
    depends_on:
      - mastra-fastify
    profiles: [mastra-fastify]

  strands-python-frontend:
    build: ./strands-python/frontend
    ports:
      - "5003:5003"
    environment:
      - VITE_BACKEND_URL=http://localhost:4003
      - VITE_THEME_COLOR=#3b82f6
      - VITE_FRAMEWORK_NAME=strands-python
    volumes:
      - ./strands-python/frontend:/app
      - /app/node_modules
    depends_on:
      - strands-python
    profiles: [strands-python]

  strands-typescript-frontend:
    build: ./strands-typescript/frontend
    ports:
      - "5004:5004"
    environment:
      - VITE_BACKEND_URL=http://localhost:4004
      - VITE_THEME_COLOR=#06b6d4
      - VITE_FRAMEWORK_NAME=strands-typescript
    volumes:
      - ./strands-typescript/frontend:/app
      - /app/node_modules
    depends_on:
      - strands-typescript
    profiles: [strands-typescript]

  claude-agent-sdk-frontend:
    build: ./claude-agent-sdk/frontend
    ports:
      - "5005:5005"
    environment:
      - VITE_BACKEND_URL=http://localhost:4005
      - VITE_THEME_COLOR=#f97316
      - VITE_FRAMEWORK_NAME=claude-agent-sdk
    volumes:
      - ./claude-agent-sdk/frontend:/app
      - /app/node_modules
    depends_on:
      - claude-agent-sdk
    profiles: [claude-agent-sdk]
