'use strict';

var chunk7XAECHYL_cjs = require('./chunk-7XAECHYL.cjs');

// src/base.ts
var MastraBase = class {
  component = chunk7XAECHYL_cjs.RegisteredLogger.LLM;
  logger;
  name;
  #rawConfig;
  constructor({
    component,
    name,
    rawConfig
  }) {
    this.component = component || chunk7XAECHYL_cjs.RegisteredLogger.LLM;
    this.name = name;
    this.#rawConfig = rawConfig;
    this.logger = new chunk7XAECHYL_cjs.ConsoleLogger({ name: `${this.component} - ${this.name}` });
  }
  /**
   * Returns the raw storage configuration this primitive was created from,
   * or undefined if it was created from code.
   */
  toRawConfig() {
    return this.#rawConfig;
  }
  /**
   * Sets the raw storage configuration for this primitive.
   * @internal
   */
  __setRawConfig(rawConfig) {
    this.#rawConfig = rawConfig;
  }
  /**
   * Set the logger for the agent
   * @param logger
   */
  __setLogger(logger) {
    this.logger = logger;
    if (this.component !== chunk7XAECHYL_cjs.RegisteredLogger.LLM) {
      this.logger.debug(`Logger updated [component=${this.component}] [name=${this.name}]`);
    }
  }
};

exports.MastraBase = MastraBase;
//# sourceMappingURL=chunk-RO47SMI7.cjs.map
//# sourceMappingURL=chunk-RO47SMI7.cjs.map