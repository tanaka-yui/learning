import type { StorageThreadType } from '../../memory/types.js';
import type { StorageAgentType, StorageMCPClientType, StorageMCPServerType, StorageMessageType, StoragePromptBlockType, StorageResourceType, StorageScorerDefinitionType, StorageWorkspaceType, StorageSkillType, StorageWorkflowRun, ObservationalMemoryRecord, DatasetRecord, DatasetItemRow, DatasetVersion, Experiment, ExperimentResult } from '../types.js';
import type { AgentVersion } from './agents/index.js';
import type { MCPClientVersion } from './mcp-clients/index.js';
import type { MCPServerVersion } from './mcp-servers/index.js';
import type { TraceEntry } from './observability/index.js';
import type { FeedbackRecord } from './observability/feedback.js';
import type { LogRecord } from './observability/logs.js';
import type { MetricRecord } from './observability/metrics.js';
import type { ScoreRecord } from './observability/scores.js';
import type { PromptBlockVersion } from './prompt-blocks/index.js';
import type { ScorerDefinitionVersion } from './scorer-definitions/index.js';
import type { SkillVersion } from './skills/index.js';
import type { WorkspaceVersion } from './workspaces/index.js';
/**
 * InMemoryDB is a thin database layer for in-memory storage.
 * It holds all the Maps that store data, similar to how a real database
 * connection (pg-promise client, libsql client) is shared across domains.
 *
 * Each domain receives a reference to this db and operates on the relevant Maps.
 */
export declare class InMemoryDB {
    readonly threads: Map<string, StorageThreadType>;
    readonly messages: Map<string, StorageMessageType>;
    readonly resources: Map<string, StorageResourceType>;
    readonly workflows: Map<string, StorageWorkflowRun>;
    readonly scores: Map<string, {
        createdAt: Date;
        updatedAt: Date | null;
        id: string;
        scorerId: string;
        entityId: string;
        runId: string;
        output: unknown;
        score: number;
        scorer: Record<string, unknown>;
        source: "LIVE" | "TEST";
        entity: Record<string, unknown>;
        input?: unknown;
        additionalContext?: Record<string, unknown> | undefined;
        requestContext?: Record<string, unknown> | undefined;
        extractStepResult?: Record<string, unknown> | undefined;
        extractPrompt?: string | undefined;
        analyzeStepResult?: Record<string, unknown> | undefined;
        analyzePrompt?: string | undefined;
        reason?: string | undefined;
        reasonPrompt?: string | undefined;
        metadata?: Record<string, unknown> | undefined;
        entityType?: string | undefined;
        structuredOutput?: boolean | undefined;
        traceId?: string | undefined;
        spanId?: string | undefined;
        resourceId?: string | undefined;
        threadId?: string | undefined;
        preprocessStepResult?: Record<string, unknown> | undefined;
        preprocessPrompt?: string | undefined;
        generateScorePrompt?: string | undefined;
        generateReasonPrompt?: string | undefined;
    }>;
    readonly traces: Map<string, TraceEntry>;
    readonly metricRecords: MetricRecord[];
    readonly logRecords: LogRecord[];
    readonly scoreRecords: ScoreRecord[];
    readonly feedbackRecords: FeedbackRecord[];
    readonly agents: Map<string, StorageAgentType>;
    readonly agentVersions: Map<string, AgentVersion>;
    readonly promptBlocks: Map<string, StoragePromptBlockType>;
    readonly promptBlockVersions: Map<string, PromptBlockVersion>;
    readonly scorerDefinitions: Map<string, StorageScorerDefinitionType>;
    readonly scorerDefinitionVersions: Map<string, ScorerDefinitionVersion>;
    readonly mcpClients: Map<string, StorageMCPClientType>;
    readonly mcpClientVersions: Map<string, MCPClientVersion>;
    readonly mcpServers: Map<string, StorageMCPServerType>;
    readonly mcpServerVersions: Map<string, MCPServerVersion>;
    readonly workspaces: Map<string, StorageWorkspaceType>;
    readonly workspaceVersions: Map<string, WorkspaceVersion>;
    readonly skills: Map<string, StorageSkillType>;
    readonly skillVersions: Map<string, SkillVersion>;
    /** Observational memory records, keyed by resourceId, each holding array of records (generations) */
    readonly observationalMemory: Map<string, ObservationalMemoryRecord[]>;
    readonly datasets: Map<string, DatasetRecord>;
    readonly datasetItems: Map<string, DatasetItemRow[]>;
    readonly datasetVersions: Map<string, DatasetVersion>;
    readonly experiments: Map<string, Experiment>;
    readonly experimentResults: Map<string, ExperimentResult>;
    /**
     * Clears all data from all collections.
     * Useful for testing.
     */
    clear(): void;
}
//# sourceMappingURL=inmemory-db.d.ts.map