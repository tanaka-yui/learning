# 07_network Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the 07_network learning chapter: six self-contained Go servers (`tcp-echo`, `udp-echo`, `http`, `dns`, `lpp`, `websocket`) plus 8 explanatory docs, demonstrating how L5/L6/L7 sit on top of TCP/UDP.

**Architecture:** Each server is an independent Go module (stdlib only) under `07_network/servers/`, tied together by `go.work`, `docker-compose.yml`, and `Makefile`. Each server has its own Dockerfile and exposes one port. Docs in `07_network/docs/` reference real code line numbers.

**Tech Stack:** Go 1.26 (`net`, `net/http`, `encoding/binary`, `crypto/sha1`, `encoding/base64`, `log/slog`), Docker Compose, Make. No third-party libraries.

**Spec:** `docs/superpowers/specs/2026-05-28-07-network-design.md`

**Phase ordering:**
- Phase 0: Skeleton (creates the chapter shell — must be first)
- Phase 1–6: One phase per server (independent, can be parallelized between subagents)
- Phase 7–9: Docs (need code in place for line references)
- Phase 10: Finalization (top-level README, smoke test, link from learning/ README)

---

## Phase 0: Chapter skeleton

**Files:**
- Create: `07_network/README.md`
- Create: `07_network/go.work`
- Create: `07_network/docker-compose.yml`
- Create: `07_network/Makefile`
- Create: `07_network/docs/.gitkeep`
- Create: `07_network/servers/.gitkeep`

### Task 0.1: Create top-level skeleton

- [ ] **Step 1: Create directories**

```bash
mkdir -p 07_network/docs 07_network/servers
touch 07_network/docs/.gitkeep 07_network/servers/.gitkeep
```

- [ ] **Step 2: Create `07_network/go.work` (initially empty; phases append `use` lines)**

```
go 1.26
```

- [ ] **Step 3: Create `07_network/README.md` placeholder**

```markdown
# 07_network: ネットワーク学習プロジェクト

TCP/UDP の上に HTTP/DNS/独自プロトコル/WebSocket が「足し算」で乗っていることを Go の `net` パッケージで体感する章。

（このファイルは Phase 10 で完成します）
```

- [ ] **Step 4: Create `07_network/docker-compose.yml` skeleton**

```yaml
services: {}
```

- [ ] **Step 5: Create `07_network/Makefile` skeleton**

```makefile
.PHONY: up down logs test test-race

up:        ; docker compose up -d --build
down:      ; docker compose down
logs:      ; docker compose logs -f
test:      ; go test ./servers/...
test-race: ; go test -race ./servers/...
```

- [ ] **Step 6: Commit**

```bash
git add 07_network/
git commit -m "network(plan): scaffold 07_network chapter skeleton"
```

---

## Phase 1: tcp-echo server

**Files:**
- Create: `07_network/servers/tcp-echo/go.mod`
- Create: `07_network/servers/tcp-echo/main.go`
- Create: `07_network/servers/tcp-echo/server.go`
- Create: `07_network/servers/tcp-echo/server_test.go`
- Create: `07_network/servers/tcp-echo/Dockerfile`
- Modify: `07_network/go.work` (add `use ./servers/tcp-echo`)
- Modify: `07_network/docker-compose.yml` (add `tcp-echo` service)
- Modify: `07_network/Makefile` (add `demo-tcp` target)

### Task 1.1: Module init and failing integration test

- [ ] **Step 1: Init module**

```bash
cd 07_network/servers/tcp-echo
go mod init example.com/network/tcp-echo
go mod edit -go=1.26
cd -
```

- [ ] **Step 2: Write failing integration test `server_test.go`**

```go
package main

import (
	"io"
	"net"
	"testing"
	"time"
)

func TestServerEchoesBytes(t *testing.T) {
	ln, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	srv := NewServer(ln)
	go srv.Serve()
	t.Cleanup(func() { _ = ln.Close() })

	conn, err := net.Dial("tcp", ln.Addr().String())
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { _ = conn.Close() })

	want := []byte("hello world\n")
	if _, err := conn.Write(want); err != nil {
		t.Fatal(err)
	}

	got := make([]byte, len(want))
	_ = conn.SetReadDeadline(time.Now().Add(2 * time.Second))
	if _, err := io.ReadFull(conn, got); err != nil {
		t.Fatal(err)
	}
	if string(got) != string(want) {
		t.Fatalf("got %q want %q", got, want)
	}
}
```

- [ ] **Step 3: Run to confirm fail**

```bash
cd 07_network/servers/tcp-echo && go test -run TestServerEchoesBytes -v
```

Expected: FAIL with `undefined: NewServer`.

### Task 1.2: Implement Server

- [ ] **Step 1: Create `server.go`**

```go
package main

import (
	"errors"
	"io"
	"log/slog"
	"net"
)

type Server struct {
	ln  net.Listener
	log *slog.Logger
}

func NewServer(ln net.Listener) *Server {
	return &Server{ln: ln, log: slog.Default()}
}

func (s *Server) Serve() error {
	for {
		conn, err := s.ln.Accept()
		if err != nil {
			if errors.Is(err, net.ErrClosed) {
				return nil
			}
			return err
		}
		s.log.Info("accept", "remote", conn.RemoteAddr().String())
		go s.handle(conn)
	}
}

func (s *Server) handle(conn net.Conn) {
	defer conn.Close()
	buf := make([]byte, 4096)
	for {
		n, err := conn.Read(buf)
		if n > 0 {
			s.log.Info("read", "n", n)
			if _, werr := conn.Write(buf[:n]); werr != nil {
				s.log.Info("write_err", "err", werr.Error())
				return
			}
		}
		if err != nil {
			if !errors.Is(err, io.EOF) {
				s.log.Info("read_err", "err", err.Error())
			}
			return
		}
	}
}
```

- [ ] **Step 2: Run test to confirm pass**

```bash
cd 07_network/servers/tcp-echo && go test -run TestServerEchoesBytes -v
```

Expected: PASS.

### Task 1.3: Create main and Dockerfile

- [ ] **Step 1: Create `main.go`**

```go
package main

import (
	"context"
	"flag"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	addr := flag.String("addr", ":9001", "listen address")
	flag.Parse()

	ln, err := net.Listen("tcp", *addr)
	if err != nil {
		slog.Error("listen", "err", err.Error())
		os.Exit(1)
	}
	slog.Info("listening", "addr", ln.Addr().String())

	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()

	srv := NewServer(ln)
	go func() {
		<-ctx.Done()
		_ = ln.Close()
	}()
	_ = srv.Serve()
}
```

- [ ] **Step 2: Create `Dockerfile`**

```dockerfile
FROM golang:1.26-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/tcp-echo .

FROM gcr.io/distroless/static
COPY --from=build /out/tcp-echo /tcp-echo
EXPOSE 9001
ENTRYPOINT ["/tcp-echo"]
```

- [ ] **Step 3: Verify build works locally**

```bash
cd 07_network/servers/tcp-echo && go build ./...
```

Expected: no output, exit 0.

### Task 1.4: Wire into workspace and compose

- [ ] **Step 1: Update `07_network/go.work` to add the module**

```
go 1.26

use ./servers/tcp-echo
```

- [ ] **Step 2: Update `07_network/docker-compose.yml`**

```yaml
services:
  tcp-echo:
    build: ./servers/tcp-echo
    ports: ["9001:9001"]
```

- [ ] **Step 3: Append `demo-tcp` target to `07_network/Makefile`**

```makefile
.PHONY: demo-tcp
demo-tcp:
	echo "hello" | nc -w1 localhost 9001
```

- [ ] **Step 4: Verify Docker build works**

```bash
cd 07_network && docker compose build tcp-echo
```

Expected: build succeeds.

- [ ] **Step 5: Commit**

```bash
cd 07_network/.. && git add 07_network/
git commit -m "network(tcp-echo): implement raw TCP echo server"
```

---

## Phase 2: udp-echo server

**Files:**
- Create: `07_network/servers/udp-echo/{go.mod,main.go,server.go,server_test.go,Dockerfile}`
- Modify: `07_network/go.work`, `07_network/docker-compose.yml`, `07_network/Makefile`

### Task 2.1: Module init and failing integration test

- [ ] **Step 1: Init module**

```bash
cd 07_network/servers/udp-echo
go mod init example.com/network/udp-echo
go mod edit -go=1.26
cd -
```

- [ ] **Step 2: Write failing test `server_test.go`**

```go
package main

import (
	"net"
	"testing"
	"time"
)

func TestServerEchoesDatagram(t *testing.T) {
	pc, err := net.ListenPacket("udp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	srv := NewServer(pc)
	go srv.Serve()
	t.Cleanup(func() { _ = pc.Close() })

	client, err := net.ListenPacket("udp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { _ = client.Close() })

	want := []byte("hello world")
	if _, err := client.WriteTo(want, pc.LocalAddr()); err != nil {
		t.Fatal(err)
	}

	got := make([]byte, 1024)
	_ = client.SetReadDeadline(time.Now().Add(2 * time.Second))
	n, _, err := client.ReadFrom(got)
	if err != nil {
		t.Fatal(err)
	}
	if string(got[:n]) != string(want) {
		t.Fatalf("got %q want %q", got[:n], want)
	}
}
```

- [ ] **Step 3: Run to confirm fail**

```bash
cd 07_network/servers/udp-echo && go test -v
```

Expected: FAIL with `undefined: NewServer`.

### Task 2.2: Implement Server

- [ ] **Step 1: Create `server.go`**

```go
package main

import (
	"errors"
	"log/slog"
	"net"
)

type Server struct {
	pc  net.PacketConn
	log *slog.Logger
}

func NewServer(pc net.PacketConn) *Server {
	return &Server{pc: pc, log: slog.Default()}
}

func (s *Server) Serve() error {
	buf := make([]byte, 65536)
	for {
		n, addr, err := s.pc.ReadFrom(buf)
		if err != nil {
			if errors.Is(err, net.ErrClosed) {
				return nil
			}
			return err
		}
		s.log.Info("read", "n", n, "from", addr.String())
		if _, werr := s.pc.WriteTo(buf[:n], addr); werr != nil {
			s.log.Info("write_err", "err", werr.Error())
		}
	}
}
```

- [ ] **Step 2: Run test to confirm pass**

```bash
cd 07_network/servers/udp-echo && go test -v
```

Expected: PASS.

### Task 2.3: Create main and Dockerfile

- [ ] **Step 1: Create `main.go`**

```go
package main

import (
	"context"
	"flag"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	addr := flag.String("addr", ":9002", "listen address")
	flag.Parse()

	pc, err := net.ListenPacket("udp", *addr)
	if err != nil {
		slog.Error("listen", "err", err.Error())
		os.Exit(1)
	}
	slog.Info("listening", "addr", pc.LocalAddr().String())

	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()

	srv := NewServer(pc)
	go func() {
		<-ctx.Done()
		_ = pc.Close()
	}()
	_ = srv.Serve()
}
```

- [ ] **Step 2: Create `Dockerfile`**

```dockerfile
FROM golang:1.26-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/udp-echo .

FROM gcr.io/distroless/static
COPY --from=build /out/udp-echo /udp-echo
EXPOSE 9002/udp
ENTRYPOINT ["/udp-echo"]
```

- [ ] **Step 3: Verify local build**

```bash
cd 07_network/servers/udp-echo && go build ./...
```

Expected: exit 0.

### Task 2.4: Wire into workspace and compose

- [ ] **Step 1: Append to `07_network/go.work`**

```
go 1.26

use ./servers/tcp-echo
use ./servers/udp-echo
```

- [ ] **Step 2: Append `udp-echo` service to `07_network/docker-compose.yml`**

```yaml
services:
  tcp-echo:
    build: ./servers/tcp-echo
    ports: ["9001:9001"]
  udp-echo:
    build: ./servers/udp-echo
    ports: ["9002:9002/udp"]
```

- [ ] **Step 3: Append `demo-udp` to `07_network/Makefile`**

```makefile
.PHONY: demo-udp
demo-udp:
	echo "hello" | nc -u -w1 localhost 9002
```

- [ ] **Step 4: Commit**

```bash
git add 07_network/
git commit -m "network(udp-echo): implement raw UDP echo server"
```

---

## Phase 3: http server (self-parsed HTTP/1.1)

**Files:**
- Create: `07_network/servers/http/{go.mod,main.go,server.go,http.go,http_test.go,server_test.go,Dockerfile}`
- Modify: `07_network/go.work`, `07_network/docker-compose.yml`, `07_network/Makefile`

### Task 3.1: Module init and request parser tests

- [ ] **Step 1: Init module**

```bash
cd 07_network/servers/http
go mod init example.com/network/http
go mod edit -go=1.26
cd -
```

- [ ] **Step 2: Write failing parser tests `http_test.go`**

```go
package main

import (
	"bufio"
	"strings"
	"testing"
)

func TestParseRequest(t *testing.T) {
	cases := []struct {
		name    string
		raw     string
		wantOK  bool
		method  string
		path    string
		host    string
		closeIt bool
	}{
		{
			name:   "simple GET",
			raw:    "GET / HTTP/1.1\r\nHost: example.com\r\n\r\n",
			wantOK: true, method: "GET", path: "/", host: "example.com",
		},
		{
			name:    "connection close",
			raw:     "GET /a HTTP/1.1\r\nHost: x\r\nConnection: close\r\n\r\n",
			wantOK:  true, method: "GET", path: "/a", host: "x", closeIt: true,
		},
		{
			name:   "malformed request line",
			raw:    "GET/HTTP/1.1\r\n\r\n",
			wantOK: false,
		},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			r := bufio.NewReader(strings.NewReader(c.raw))
			req, err := ParseRequest(r)
			if c.wantOK && err != nil {
				t.Fatalf("unexpected err: %v", err)
			}
			if !c.wantOK {
				if err == nil {
					t.Fatal("expected error")
				}
				return
			}
			if req.Method != c.method || req.Path != c.path || req.Host != c.host || req.Close != c.closeIt {
				t.Fatalf("got %+v", req)
			}
		})
	}
}
```

- [ ] **Step 3: Run to confirm fail**

```bash
cd 07_network/servers/http && go test -run TestParseRequest -v
```

Expected: FAIL with `undefined: ParseRequest`.

### Task 3.2: Implement request parser

- [ ] **Step 1: Create `http.go`**

```go
package main

import (
	"bufio"
	"errors"
	"strings"
)

type Request struct {
	Method  string
	Path    string
	Proto   string
	Host    string
	Headers map[string]string
	Close   bool
}

// ParseRequest reads a single HTTP/1.x request from r.
// L7: method/path semantics. L6: text framing (lines + CRLF). L5: Host + Connection.
func ParseRequest(r *bufio.Reader) (*Request, error) {
	line, err := r.ReadString('\n')
	if err != nil {
		return nil, err
	}
	line = strings.TrimRight(line, "\r\n")
	parts := strings.Split(line, " ")
	if len(parts) != 3 {
		return nil, errors.New("malformed request line")
	}
	req := &Request{Method: parts[0], Path: parts[1], Proto: parts[2], Headers: map[string]string{}}

	for {
		l, err := r.ReadString('\n')
		if err != nil {
			return nil, err
		}
		l = strings.TrimRight(l, "\r\n")
		if l == "" {
			break
		}
		k, v, ok := strings.Cut(l, ":")
		if !ok {
			return nil, errors.New("malformed header")
		}
		k = strings.ToLower(strings.TrimSpace(k))
		v = strings.TrimSpace(v)
		req.Headers[k] = v
		switch k {
		case "host":
			req.Host = v
		case "connection":
			if strings.EqualFold(v, "close") {
				req.Close = true
			}
		}
	}
	return req, nil
}
```

- [ ] **Step 2: Run parser tests**

```bash
cd 07_network/servers/http && go test -run TestParseRequest -v
```

Expected: PASS.

### Task 3.3: Server integration test

- [ ] **Step 1: Write failing server test `server_test.go`**

```go
package main

import (
	"bufio"
	"net"
	"net/http"
	"strings"
	"testing"
	"time"
)

func TestServer(t *testing.T) {
	ln, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	srv := NewServer(ln)
	go srv.Serve()
	t.Cleanup(func() { _ = ln.Close() })

	cases := []struct {
		name      string
		req       string
		wantCode  int
		wantBody  string
	}{
		{"root", "GET / HTTP/1.1\r\nHost: x\r\n\r\n", 200, "Hello, world!\n"},
		{"not found", "GET /missing HTTP/1.1\r\nHost: x\r\n\r\n", 404, ""},
		{"method not allowed", "POST / HTTP/1.1\r\nHost: x\r\nContent-Length: 0\r\n\r\n", 405, ""},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			conn, err := net.Dial("tcp", ln.Addr().String())
			if err != nil {
				t.Fatal(err)
			}
			defer conn.Close()
			_ = conn.SetDeadline(time.Now().Add(2 * time.Second))
			if _, err := conn.Write([]byte(c.req)); err != nil {
				t.Fatal(err)
			}
			resp, err := http.ReadResponse(bufio.NewReader(conn), nil)
			if err != nil {
				t.Fatal(err)
			}
			if resp.StatusCode != c.wantCode {
				t.Fatalf("status %d want %d", resp.StatusCode, c.wantCode)
			}
			body := make([]byte, 64)
			n, _ := resp.Body.Read(body)
			if c.wantBody != "" && !strings.HasPrefix(string(body[:n]), c.wantBody) {
				t.Fatalf("body %q want prefix %q", body[:n], c.wantBody)
			}
		})
	}
}
```

- [ ] **Step 2: Run to confirm fail**

```bash
cd 07_network/servers/http && go test -run TestServer -v
```

Expected: FAIL with `undefined: NewServer`.

### Task 3.4: Implement Server

- [ ] **Step 1: Create `server.go`**

```go
package main

import (
	"bufio"
	"errors"
	"fmt"
	"log/slog"
	"net"
)

type Server struct {
	ln  net.Listener
	log *slog.Logger
}

func NewServer(ln net.Listener) *Server {
	return &Server{ln: ln, log: slog.Default()}
}

func (s *Server) Serve() error {
	for {
		conn, err := s.ln.Accept()
		if err != nil {
			if errors.Is(err, net.ErrClosed) {
				return nil
			}
			return err
		}
		go s.handle(conn)
	}
}

func (s *Server) handle(conn net.Conn) {
	defer conn.Close()
	r := bufio.NewReader(conn)
	req, err := ParseRequest(r)
	if err != nil {
		s.log.Info("parse_err", "err", err.Error())
		writeResponse(conn, 400, "text/plain", []byte("bad request\n"), true)
		return
	}
	s.log.Info("L5: parsed request", "host", req.Host, "close", req.Close)
	s.log.Info("L7: route", "method", req.Method, "path", req.Path)

	switch {
	case req.Method != "GET":
		writeResponse(conn, 405, "text/plain", nil, true)
	case req.Path == "/":
		writeResponse(conn, 200, "text/plain", []byte("Hello, world!\n"), req.Close)
	default:
		writeResponse(conn, 404, "text/plain", nil, true)
	}
}

// writeResponse builds an HTTP/1.1 response by hand.
// L6: Content-Type / Content-Length headers.
func writeResponse(conn net.Conn, code int, contentType string, body []byte, closeIt bool) {
	reason := map[int]string{200: "OK", 400: "Bad Request", 404: "Not Found", 405: "Method Not Allowed"}[code]
	hdr := fmt.Sprintf("HTTP/1.1 %d %s\r\nContent-Type: %s\r\nContent-Length: %d\r\n",
		code, reason, contentType, len(body))
	if closeIt {
		hdr += "Connection: close\r\n"
	}
	hdr += "\r\n"
	_, _ = conn.Write([]byte(hdr))
	if len(body) > 0 {
		_, _ = conn.Write(body)
	}
}
```

- [ ] **Step 2: Run server tests**

```bash
cd 07_network/servers/http && go test -v
```

Expected: PASS for all three subtests.

### Task 3.5: Create main and Dockerfile

- [ ] **Step 1: Create `main.go`** (same shape as Phase 1's `main.go`, just swap `addr` default to `:9003`)

```go
package main

import (
	"context"
	"flag"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	addr := flag.String("addr", ":9003", "listen address")
	flag.Parse()

	ln, err := net.Listen("tcp", *addr)
	if err != nil {
		slog.Error("listen", "err", err.Error())
		os.Exit(1)
	}
	slog.Info("listening", "addr", ln.Addr().String())

	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()

	srv := NewServer(ln)
	go func() {
		<-ctx.Done()
		_ = ln.Close()
	}()
	_ = srv.Serve()
}
```

- [ ] **Step 2: Create `Dockerfile`**

```dockerfile
FROM golang:1.26-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/http .

FROM gcr.io/distroless/static
COPY --from=build /out/http /http
EXPOSE 9003
ENTRYPOINT ["/http"]
```

- [ ] **Step 3: Verify build**

```bash
cd 07_network/servers/http && go build ./...
```

Expected: exit 0.

### Task 3.6: Wire and commit

- [ ] **Step 1: Append `use ./servers/http` to `go.work`**

- [ ] **Step 2: Append `http` service to `docker-compose.yml`**

```yaml
  http:
    build: ./servers/http
    ports: ["9003:9003"]
```

- [ ] **Step 3: Append to `Makefile`**

```makefile
.PHONY: demo-http
demo-http:
	curl -v http://localhost:9003/
```

- [ ] **Step 4: Commit**

```bash
git add 07_network/
git commit -m "network(http): implement self-parsed HTTP/1.1 server"
```

---

## Phase 4: dns server (self-parsed DNS A query)

**Files:**
- Create: `07_network/servers/dns/{go.mod,main.go,server.go,wire.go,wire_test.go,server_test.go,Dockerfile}`
- Modify: `07_network/go.work`, `07_network/docker-compose.yml`, `07_network/Makefile`

### Task 4.1: Module init and wire format tests

- [ ] **Step 1: Init module**

```bash
cd 07_network/servers/dns
go mod init example.com/network/dns
go mod edit -go=1.26
cd -
```

- [ ] **Step 2: Write failing wire tests `wire_test.go`**

```go
package main

import (
	"bytes"
	"testing"
)

func TestDecodeQNAME(t *testing.T) {
	cases := []struct {
		name string
		raw  []byte
		want string
		wantErr bool
	}{
		{"single label", []byte{3, 'w', 'w', 'w', 0}, "www", false},
		{"two labels", []byte{7, 'e', 'x', 'a', 'm', 'p', 'l', 'e', 3, 'c', 'o', 'm', 0}, "example.com", false},
		{"pointer (unsupported)", []byte{0xC0, 0x00}, "", true},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			r := bytes.NewReader(c.raw)
			name, err := DecodeQNAME(r)
			if c.wantErr {
				if err == nil {
					t.Fatal("want err")
				}
				return
			}
			if err != nil {
				t.Fatal(err)
			}
			if name != c.want {
				t.Fatalf("got %q want %q", name, c.want)
			}
		})
	}
}

func TestEncodeQNAMERoundtrip(t *testing.T) {
	encoded := EncodeQNAME("example.com")
	got, err := DecodeQNAME(bytes.NewReader(encoded))
	if err != nil {
		t.Fatal(err)
	}
	if got != "example.com" {
		t.Fatalf("got %q", got)
	}
}

func TestParseHeader(t *testing.T) {
	raw := []byte{
		0x12, 0x34, // ID
		0x01, 0x00, // flags: RD
		0x00, 0x01, // QDCOUNT
		0x00, 0x00, // ANCOUNT
		0x00, 0x00, // NSCOUNT
		0x00, 0x00, // ARCOUNT
	}
	h, err := ParseHeader(raw)
	if err != nil {
		t.Fatal(err)
	}
	if h.ID != 0x1234 || h.QDCount != 1 {
		t.Fatalf("got %+v", h)
	}
}
```

- [ ] **Step 3: Run to confirm fail**

```bash
cd 07_network/servers/dns && go test -v
```

Expected: FAIL with `undefined: DecodeQNAME` etc.

### Task 4.2: Implement wire format

- [ ] **Step 1: Create `wire.go`**

```go
package main

import (
	"encoding/binary"
	"errors"
	"io"
	"net"
	"strings"
)

// Header is the 12-byte DNS message header.
// L6: bit-packed wire layout.
type Header struct {
	ID      uint16
	Flags   uint16
	QDCount uint16
	ANCount uint16
	NSCount uint16
	ARCount uint16
}

func ParseHeader(b []byte) (Header, error) {
	if len(b) < 12 {
		return Header{}, errors.New("short header")
	}
	return Header{
		ID:      binary.BigEndian.Uint16(b[0:2]),
		Flags:   binary.BigEndian.Uint16(b[2:4]),
		QDCount: binary.BigEndian.Uint16(b[4:6]),
		ANCount: binary.BigEndian.Uint16(b[6:8]),
		NSCount: binary.BigEndian.Uint16(b[8:10]),
		ARCount: binary.BigEndian.Uint16(b[10:12]),
	}, nil
}

func (h Header) Encode() []byte {
	b := make([]byte, 12)
	binary.BigEndian.PutUint16(b[0:2], h.ID)
	binary.BigEndian.PutUint16(b[2:4], h.Flags)
	binary.BigEndian.PutUint16(b[4:6], h.QDCount)
	binary.BigEndian.PutUint16(b[6:8], h.ANCount)
	binary.BigEndian.PutUint16(b[8:10], h.NSCount)
	binary.BigEndian.PutUint16(b[10:12], h.ARCount)
	return b
}

// DecodeQNAME reads a label-encoded name. Compressed pointers are rejected
// (this implementation supports compression on the encode side only).
// L6: label-length prefix encoding.
func DecodeQNAME(r io.ByteReader) (string, error) {
	var parts []string
	for {
		n, err := r.ReadByte()
		if err != nil {
			return "", err
		}
		if n == 0 {
			return strings.Join(parts, "."), nil
		}
		if n&0xC0 != 0 {
			return "", errors.New("compressed pointer not supported in decoder")
		}
		buf := make([]byte, n)
		for i := range buf {
			b, err := r.ReadByte()
			if err != nil {
				return "", err
			}
			buf[i] = b
		}
		parts = append(parts, string(buf))
	}
}

func EncodeQNAME(name string) []byte {
	var out []byte
	for _, label := range strings.Split(name, ".") {
		if label == "" {
			continue
		}
		out = append(out, byte(len(label)))
		out = append(out, []byte(label)...)
	}
	out = append(out, 0)
	return out
}

// EncodeAnswer builds a single A record answer.
// L6: type/class/ttl/rdlength bit packing.
func EncodeAnswer(name string, ttl uint32, ip net.IP) []byte {
	var out []byte
	out = append(out, EncodeQNAME(name)...)
	t := make([]byte, 10)
	binary.BigEndian.PutUint16(t[0:2], 1) // TYPE A
	binary.BigEndian.PutUint16(t[2:4], 1) // CLASS IN
	binary.BigEndian.PutUint32(t[4:8], ttl)
	binary.BigEndian.PutUint16(t[8:10], 4) // RDLENGTH
	out = append(out, t...)
	out = append(out, ip.To4()...)
	return out
}
```

- [ ] **Step 2: Run wire tests**

```bash
cd 07_network/servers/dns && go test -run 'TestDecodeQNAME|TestEncodeQNAMERoundtrip|TestParseHeader' -v
```

Expected: PASS.

### Task 4.3: Server integration test

- [ ] **Step 1: Write failing `server_test.go`**

```go
package main

import (
	"bytes"
	"encoding/binary"
	"net"
	"testing"
	"time"
)

func TestServerAnswersAQuery(t *testing.T) {
	pc, err := net.ListenPacket("udp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	srv := NewServer(pc, map[string]net.IP{"example.local": net.ParseIP("10.0.0.1")})
	go srv.Serve()
	t.Cleanup(func() { _ = pc.Close() })

	// Build a query for example.local A IN with TXID 0xABCD.
	var q bytes.Buffer
	hdr := Header{ID: 0xABCD, Flags: 0x0100, QDCount: 1}
	q.Write(hdr.Encode())
	q.Write(EncodeQNAME("example.local"))
	typeClass := make([]byte, 4)
	binary.BigEndian.PutUint16(typeClass[0:2], 1)
	binary.BigEndian.PutUint16(typeClass[2:4], 1)
	q.Write(typeClass)

	client, err := net.ListenPacket("udp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { _ = client.Close() })
	if _, err := client.WriteTo(q.Bytes(), pc.LocalAddr()); err != nil {
		t.Fatal(err)
	}

	resp := make([]byte, 512)
	_ = client.SetReadDeadline(time.Now().Add(2 * time.Second))
	n, _, err := client.ReadFrom(resp)
	if err != nil {
		t.Fatal(err)
	}
	if n < 12 {
		t.Fatalf("short response: %d bytes", n)
	}
	got, _ := ParseHeader(resp[:12])
	if got.ID != 0xABCD {
		t.Fatalf("TXID mismatch: %x", got.ID)
	}
	if got.ANCount != 1 {
		t.Fatalf("ANCount %d want 1", got.ANCount)
	}
	if !bytes.Contains(resp[:n], []byte{10, 0, 0, 1}) {
		t.Fatalf("answer does not contain 10.0.0.1: %x", resp[:n])
	}
}
```

- [ ] **Step 2: Run to confirm fail**

```bash
cd 07_network/servers/dns && go test -run TestServerAnswersAQuery -v
```

Expected: FAIL with `undefined: NewServer`.

### Task 4.4: Implement Server

- [ ] **Step 1: Create `server.go`**

```go
package main

import (
	"bytes"
	"encoding/binary"
	"errors"
	"log/slog"
	"net"
)

type Server struct {
	pc   net.PacketConn
	zone map[string]net.IP
	log  *slog.Logger
}

func NewServer(pc net.PacketConn, zone map[string]net.IP) *Server {
	return &Server{pc: pc, zone: zone, log: slog.Default()}
}

func (s *Server) Serve() error {
	buf := make([]byte, 512)
	for {
		n, addr, err := s.pc.ReadFrom(buf)
		if err != nil {
			if errors.Is(err, net.ErrClosed) {
				return nil
			}
			return err
		}
		resp, err := s.handle(buf[:n])
		if err != nil {
			s.log.Info("handle_err", "err", err.Error())
			continue
		}
		_, _ = s.pc.WriteTo(resp, addr)
	}
}

func (s *Server) handle(msg []byte) ([]byte, error) {
	if len(msg) < 12 {
		return nil, errors.New("short message")
	}
	h, err := ParseHeader(msg[:12])
	if err != nil {
		return nil, err
	}
	s.log.Info("L5: parsed TXID", "txid", h.ID)
	if h.QDCount != 1 {
		return nil, errors.New("only single-question queries supported")
	}
	r := bytes.NewReader(msg[12:])
	name, err := DecodeQNAME(r)
	if err != nil {
		return nil, err
	}
	s.log.Info("L6: decoded QNAME", "name", name)
	tc := make([]byte, 4)
	if _, err := r.Read(tc); err != nil {
		return nil, err
	}
	qtype := binary.BigEndian.Uint16(tc[0:2])
	qclass := binary.BigEndian.Uint16(tc[2:4])
	if qtype != 1 || qclass != 1 {
		return nil, errors.New("only A/IN supported")
	}

	ip, ok := s.zone[name]
	if !ok {
		// NXDOMAIN: flags=0x8183 (QR=1, AA=1, RCODE=3)
		s.log.Info("L7: NXDOMAIN", "name", name)
		out := Header{ID: h.ID, Flags: 0x8183, QDCount: 1}.Encode()
		out = append(out, EncodeQNAME(name)...)
		out = append(out, tc...)
		return out, nil
	}
	s.log.Info("L7: answering A record", "name", name, "ip", ip.String())

	out := Header{ID: h.ID, Flags: 0x8180, QDCount: 1, ANCount: 1}.Encode()
	out = append(out, EncodeQNAME(name)...)
	out = append(out, tc...)
	out = append(out, EncodeAnswer(name, 60, ip)...)
	return out, nil
}
```

- [ ] **Step 2: Run all tests**

```bash
cd 07_network/servers/dns && go test -v
```

Expected: all PASS.

### Task 4.5: Create main and Dockerfile

- [ ] **Step 1: Create `main.go`**

```go
package main

import (
	"context"
	"flag"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	addr := flag.String("addr", ":5353", "listen address")
	flag.Parse()

	pc, err := net.ListenPacket("udp", *addr)
	if err != nil {
		slog.Error("listen", "err", err.Error())
		os.Exit(1)
	}
	slog.Info("listening", "addr", pc.LocalAddr().String())

	zone := map[string]net.IP{
		"example.local":      net.ParseIP("10.0.0.1"),
		"hello.example.local": net.ParseIP("10.0.0.2"),
	}

	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()

	srv := NewServer(pc, zone)
	go func() {
		<-ctx.Done()
		_ = pc.Close()
	}()
	_ = srv.Serve()
}
```

- [ ] **Step 2: Create `Dockerfile`**

```dockerfile
FROM golang:1.26-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/dns .

FROM gcr.io/distroless/static
COPY --from=build /out/dns /dns
EXPOSE 5353/udp
ENTRYPOINT ["/dns"]
```

- [ ] **Step 3: Verify build**

```bash
cd 07_network/servers/dns && go build ./...
```

Expected: exit 0.

### Task 4.6: Wire and commit

- [ ] **Step 1: Append `use ./servers/dns` to `go.work`**

- [ ] **Step 2: Append to `docker-compose.yml`**

```yaml
  dns:
    build: ./servers/dns
    ports: ["5353:5353/udp"]
```

- [ ] **Step 3: Append to `Makefile`**

```makefile
.PHONY: demo-dns
demo-dns:
	dig @localhost -p 5353 example.local +short
```

- [ ] **Step 4: Commit**

```bash
git add 07_network/
git commit -m "network(dns): implement self-parsed DNS A-record server"
```

---

## Phase 5: lpp server + client (length-prefixed protocol)

**Files:**
- Create: `07_network/servers/lpp/{go.mod,main.go,server.go,frame.go,frame_test.go,server_test.go,Dockerfile}`
- Create: `07_network/servers/lpp/cmd/client/main.go`
- Modify: `07_network/go.work`, `07_network/docker-compose.yml`, `07_network/Makefile`

### Task 5.1: Module init and frame codec tests

- [ ] **Step 1: Init module**

```bash
cd 07_network/servers/lpp
go mod init example.com/network/lpp
go mod edit -go=1.26
cd -
```

- [ ] **Step 2: Write failing `frame_test.go`**

```go
package main

import (
	"bytes"
	"io"
	"testing"
)

func TestEncodeDecodeFrameRoundtrip(t *testing.T) {
	cases := []struct {
		name    string
		cmd     byte
		payload []byte
	}{
		{"ping empty", CmdPing, nil},
		{"echo short", CmdEcho, []byte("hello")},
		{"time 8 bytes", CmdTime, []byte{0, 0, 0, 0, 0, 0, 0, 1}},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			var buf bytes.Buffer
			if err := WriteFrame(&buf, c.cmd, c.payload); err != nil {
				t.Fatal(err)
			}
			cmd, payload, err := ReadFrame(&buf)
			if err != nil {
				t.Fatal(err)
			}
			if cmd != c.cmd {
				t.Fatalf("cmd %x want %x", cmd, c.cmd)
			}
			if !bytes.Equal(payload, c.payload) {
				t.Fatalf("payload %x want %x", payload, c.payload)
			}
		})
	}
}

func TestReadFrameShortRead(t *testing.T) {
	// Provide only the length prefix; expect io.ErrUnexpectedEOF on body.
	buf := bytes.NewReader([]byte{0, 0, 0, 5, 0x02}) // says 5 body bytes, gives 1
	_, _, err := ReadFrame(buf)
	if err != io.ErrUnexpectedEOF {
		t.Fatalf("want ErrUnexpectedEOF got %v", err)
	}
}
```

- [ ] **Step 3: Run to confirm fail**

```bash
cd 07_network/servers/lpp && go test -v
```

Expected: FAIL with `undefined: WriteFrame` etc.

### Task 5.2: Implement frame codec

- [ ] **Step 1: Create `frame.go`**

```go
package main

import (
	"encoding/binary"
	"errors"
	"io"
)

const (
	CmdPing    byte = 0x01
	CmdEcho    byte = 0x02
	CmdTime    byte = 0x03
	CmdUnknown byte = 0xFF
)

const maxFrame = 1 << 20 // 1 MiB safety cap

// WriteFrame writes a length-prefixed frame: Len(4) | Cmd(1) | Payload.
// L6: binary length + cmd byte encoding.
func WriteFrame(w io.Writer, cmd byte, payload []byte) error {
	body := make([]byte, 1+len(payload))
	body[0] = cmd
	copy(body[1:], payload)
	hdr := make([]byte, 4)
	binary.BigEndian.PutUint32(hdr, uint32(len(body)))
	if _, err := w.Write(hdr); err != nil {
		return err
	}
	_, err := w.Write(body)
	return err
}

func ReadFrame(r io.Reader) (byte, []byte, error) {
	hdr := make([]byte, 4)
	if _, err := io.ReadFull(r, hdr); err != nil {
		return 0, nil, err
	}
	n := binary.BigEndian.Uint32(hdr)
	if n == 0 {
		return 0, nil, errors.New("empty frame")
	}
	if n > maxFrame {
		return 0, nil, errors.New("frame too large")
	}
	body := make([]byte, n)
	if _, err := io.ReadFull(r, body); err != nil {
		return 0, nil, err
	}
	return body[0], body[1:], nil
}
```

- [ ] **Step 2: Run frame tests**

```bash
cd 07_network/servers/lpp && go test -run TestEncodeDecodeFrameRoundtrip -v
```

Expected: PASS.

### Task 5.3: Server integration test

- [ ] **Step 1: Write failing `server_test.go`**

```go
package main

import (
	"bytes"
	"encoding/binary"
	"net"
	"testing"
	"time"
)

func TestServerCommands(t *testing.T) {
	ln, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	srv := NewServer(ln)
	go srv.Serve()
	t.Cleanup(func() { _ = ln.Close() })

	cases := []struct {
		name     string
		cmd      byte
		payload  []byte
		wantCmd  byte
		wantBody []byte
		bodyLen  int
	}{
		{"ping", CmdPing, nil, CmdPing, nil, 0},
		{"echo", CmdEcho, []byte("hi"), CmdEcho, []byte("hi"), 2},
		{"time returns 8 bytes", CmdTime, nil, CmdTime, nil, 8},
		{"unknown", 0xAA, nil, CmdUnknown, nil, 0},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			conn, err := net.Dial("tcp", ln.Addr().String())
			if err != nil {
				t.Fatal(err)
			}
			defer conn.Close()
			_ = conn.SetDeadline(time.Now().Add(2 * time.Second))
			if err := WriteFrame(conn, c.cmd, c.payload); err != nil {
				t.Fatal(err)
			}
			cmd, body, err := ReadFrame(conn)
			if err != nil {
				t.Fatal(err)
			}
			if cmd != c.wantCmd {
				t.Fatalf("cmd %x want %x", cmd, c.wantCmd)
			}
			if c.wantBody != nil && !bytes.Equal(body, c.wantBody) {
				t.Fatalf("body %x want %x", body, c.wantBody)
			}
			if c.bodyLen > 0 && len(body) != c.bodyLen {
				t.Fatalf("body len %d want %d", len(body), c.bodyLen)
			}
			if c.cmd == CmdTime {
				ts := int64(binary.BigEndian.Uint64(body))
				if ts <= 0 {
					t.Fatalf("invalid timestamp %d", ts)
				}
			}
		})
	}
}
```

- [ ] **Step 2: Run to confirm fail**

```bash
cd 07_network/servers/lpp && go test -run TestServerCommands -v
```

Expected: FAIL with `undefined: NewServer`.

### Task 5.4: Implement Server

- [ ] **Step 1: Create `server.go`**

```go
package main

import (
	"encoding/binary"
	"errors"
	"log/slog"
	"net"
	"time"
)

type Server struct {
	ln  net.Listener
	log *slog.Logger
}

func NewServer(ln net.Listener) *Server {
	return &Server{ln: ln, log: slog.Default()}
}

func (s *Server) Serve() error {
	for {
		conn, err := s.ln.Accept()
		if err != nil {
			if errors.Is(err, net.ErrClosed) {
				return nil
			}
			return err
		}
		go s.handle(conn)
	}
}

func (s *Server) handle(conn net.Conn) {
	defer conn.Close()
	for {
		cmd, payload, err := ReadFrame(conn)
		if err != nil {
			return
		}
		s.log.Info("L6: decoded frame", "cmd", cmd, "payload_len", len(payload))
		respCmd, respPayload := dispatch(cmd, payload)
		s.log.Info("L7: dispatch", "in", cmd, "out", respCmd)
		if err := WriteFrame(conn, respCmd, respPayload); err != nil {
			return
		}
	}
}

func dispatch(cmd byte, payload []byte) (byte, []byte) {
	switch cmd {
	case CmdPing:
		return CmdPing, nil
	case CmdEcho:
		return CmdEcho, payload
	case CmdTime:
		out := make([]byte, 8)
		binary.BigEndian.PutUint64(out, uint64(time.Now().UnixNano()))
		return CmdTime, out
	default:
		return CmdUnknown, nil
	}
}
```

- [ ] **Step 2: Run server tests**

```bash
cd 07_network/servers/lpp && go test -v
```

Expected: PASS.

### Task 5.5: Main, client, Dockerfile

- [ ] **Step 1: Create `main.go`**

```go
package main

import (
	"context"
	"flag"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	addr := flag.String("addr", ":9004", "listen address")
	flag.Parse()

	ln, err := net.Listen("tcp", *addr)
	if err != nil {
		slog.Error("listen", "err", err.Error())
		os.Exit(1)
	}
	slog.Info("listening", "addr", ln.Addr().String())

	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()

	srv := NewServer(ln)
	go func() {
		<-ctx.Done()
		_ = ln.Close()
	}()
	_ = srv.Serve()
}
```

- [ ] **Step 2: Create `cmd/client/main.go`**

The frame codec is duplicated here. Both `servers/lpp/` and `servers/lpp/cmd/client/` are `package main`, so they cannot import each other directly. Duplication keeps each binary self-contained.

```go
package main

import (
	"encoding/binary"
	"errors"
	"flag"
	"fmt"
	"io"
	"net"
	"os"
	"strings"
	"time"
)

const (
	cmdPing byte = 0x01
	cmdEcho byte = 0x02
	cmdTime byte = 0x03
)

func writeFrame(w io.Writer, cmd byte, payload []byte) error {
	body := make([]byte, 1+len(payload))
	body[0] = cmd
	copy(body[1:], payload)
	hdr := make([]byte, 4)
	binary.BigEndian.PutUint32(hdr, uint32(len(body)))
	if _, err := w.Write(hdr); err != nil {
		return err
	}
	_, err := w.Write(body)
	return err
}

func readFrame(r io.Reader) (byte, []byte, error) {
	hdr := make([]byte, 4)
	if _, err := io.ReadFull(r, hdr); err != nil {
		return 0, nil, err
	}
	n := binary.BigEndian.Uint32(hdr)
	if n == 0 {
		return 0, nil, errors.New("empty")
	}
	body := make([]byte, n)
	if _, err := io.ReadFull(r, body); err != nil {
		return 0, nil, err
	}
	return body[0], body[1:], nil
}

func main() {
	addr := flag.String("addr", "localhost:9004", "server address")
	flag.Parse()
	if flag.NArg() < 1 {
		fmt.Fprintln(os.Stderr, "usage: client [-addr host:port] PING|ECHO <msg>|TIME")
		os.Exit(2)
	}
	conn, err := net.Dial("tcp", *addr)
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	defer conn.Close()
	_ = conn.SetDeadline(time.Now().Add(5 * time.Second))

	var cmd byte
	var payload []byte
	switch strings.ToUpper(flag.Arg(0)) {
	case "PING":
		cmd = cmdPing
	case "ECHO":
		cmd = cmdEcho
		payload = []byte(strings.Join(flag.Args()[1:], " "))
	case "TIME":
		cmd = cmdTime
	default:
		fmt.Fprintf(os.Stderr, "unknown command %q\n", flag.Arg(0))
		os.Exit(2)
	}
	if err := writeFrame(conn, cmd, payload); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	rcmd, rbody, err := readFrame(conn)
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	switch rcmd {
	case cmdTime:
		ts := int64(binary.BigEndian.Uint64(rbody))
		fmt.Printf("TIME %s\n", time.Unix(0, ts).Format(time.RFC3339Nano))
	default:
		fmt.Printf("0x%02X %s\n", rcmd, string(rbody))
	}
}
```

- [ ] **Step 3: Create `Dockerfile`**

```dockerfile
FROM golang:1.26-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/lpp .

FROM gcr.io/distroless/static
COPY --from=build /out/lpp /lpp
EXPOSE 9004
ENTRYPOINT ["/lpp"]
```

- [ ] **Step 4: Verify builds**

```bash
cd 07_network/servers/lpp && go build ./... && go build ./cmd/client
```

Expected: exit 0.

### Task 5.6: Wire and commit

- [ ] **Step 1: Append `use ./servers/lpp` to `go.work`**

- [ ] **Step 2: Append to `docker-compose.yml`**

```yaml
  lpp:
    build: ./servers/lpp
    ports: ["9004:9004"]
```

- [ ] **Step 3: Append to `Makefile`**

```makefile
.PHONY: demo-lpp
demo-lpp:
	go run ./servers/lpp/cmd/client PING
```

- [ ] **Step 4: Commit**

```bash
git add 07_network/
git commit -m "network(lpp): implement length-prefixed binary protocol server + client"
```

---

## Phase 6: websocket server + sender + receiver

**Files:**
- Create: `07_network/servers/websocket/{go.mod,main.go,server.go,handshake.go,frame.go,hub.go,handshake_test.go,frame_test.go,hub_test.go,server_test.go,Dockerfile}`
- Create: `07_network/servers/websocket/cmd/sender/main.go`
- Create: `07_network/servers/websocket/cmd/receiver/main.go`
- Modify: `07_network/go.work`, `07_network/docker-compose.yml`, `07_network/Makefile`

### Task 6.1: Module init and handshake test

- [ ] **Step 1: Init module**

```bash
cd 07_network/servers/websocket
go mod init example.com/network/websocket
go mod edit -go=1.26
cd -
```

- [ ] **Step 2: Write failing `handshake_test.go`**

The Sec-WebSocket-Accept calculation from RFC 6455 uses the example key `dGhlIHNhbXBsZSBub25jZQ==` and expects `s3pPLMBiTxaQ9kYGzzhZRbK+xOo=`.

```go
package main

import "testing"

func TestAcceptKey(t *testing.T) {
	got := AcceptKey("dGhlIHNhbXBsZSBub25jZQ==")
	want := "s3pPLMBiTxaQ9kYGzzhZRbK+xOo="
	if got != want {
		t.Fatalf("got %q want %q", got, want)
	}
}
```

- [ ] **Step 3: Run to confirm fail**

```bash
cd 07_network/servers/websocket && go test -run TestAcceptKey -v
```

Expected: FAIL with `undefined: AcceptKey`.

### Task 6.2: Implement handshake

- [ ] **Step 1: Create `handshake.go`**

```go
package main

import (
	"bufio"
	"crypto/sha1"
	"encoding/base64"
	"errors"
	"fmt"
	"net"
	"strings"
)

const wsMagicGUID = "258EAFA5-E914-47DA-95CA-C5AB0DC85B11"

// AcceptKey computes the Sec-WebSocket-Accept response per RFC 6455.
// L5: completes the WebSocket upgrade handshake.
func AcceptKey(clientKey string) string {
	h := sha1.New()
	_, _ = h.Write([]byte(clientKey + wsMagicGUID))
	return base64.StdEncoding.EncodeToString(h.Sum(nil))
}

// Upgrade parses the HTTP upgrade request from r, writes the 101 response on conn,
// and returns the requested room name from the query string.
func Upgrade(conn net.Conn, r *bufio.Reader) (room string, err error) {
	line, err := r.ReadString('\n')
	if err != nil {
		return "", err
	}
	parts := strings.SplitN(strings.TrimRight(line, "\r\n"), " ", 3)
	if len(parts) != 3 || parts[0] != "GET" {
		return "", errors.New("not a GET request")
	}
	path := parts[1]
	if i := strings.Index(path, "?room="); i >= 0 {
		room = path[i+len("?room="):]
	}

	var key string
	for {
		l, err := r.ReadString('\n')
		if err != nil {
			return "", err
		}
		l = strings.TrimRight(l, "\r\n")
		if l == "" {
			break
		}
		k, v, ok := strings.Cut(l, ":")
		if !ok {
			continue
		}
		if strings.EqualFold(strings.TrimSpace(k), "Sec-WebSocket-Key") {
			key = strings.TrimSpace(v)
		}
	}
	if key == "" {
		return "", errors.New("missing Sec-WebSocket-Key")
	}
	resp := fmt.Sprintf(
		"HTTP/1.1 101 Switching Protocols\r\n"+
			"Upgrade: websocket\r\nConnection: Upgrade\r\n"+
			"Sec-WebSocket-Accept: %s\r\n\r\n", AcceptKey(key))
	if _, err := conn.Write([]byte(resp)); err != nil {
		return "", err
	}
	return room, nil
}
```

- [ ] **Step 2: Run handshake test**

```bash
cd 07_network/servers/websocket && go test -run TestAcceptKey -v
```

Expected: PASS.

### Task 6.3: Frame codec tests

- [ ] **Step 1: Write failing `frame_test.go`**

```go
package main

import (
	"bytes"
	"testing"
)

func TestReadMaskedTextFrame(t *testing.T) {
	// FIN=1, opcode=1 (text), MASK=1, payload len=5, key=01020304, masked payload "hello".
	mask := []byte{0x01, 0x02, 0x03, 0x04}
	plain := []byte("hello")
	masked := make([]byte, len(plain))
	for i := range plain {
		masked[i] = plain[i] ^ mask[i%4]
	}
	raw := append([]byte{0x81, 0x85}, mask...)
	raw = append(raw, masked...)

	op, payload, err := ReadFrame(bytes.NewReader(raw))
	if err != nil {
		t.Fatal(err)
	}
	if op != OpText {
		t.Fatalf("op %x", op)
	}
	if string(payload) != "hello" {
		t.Fatalf("payload %q", payload)
	}
}

func TestWriteUnmaskedTextFrame(t *testing.T) {
	var buf bytes.Buffer
	if err := WriteFrame(&buf, OpText, []byte("hi")); err != nil {
		t.Fatal(err)
	}
	want := []byte{0x81, 0x02, 'h', 'i'}
	if !bytes.Equal(buf.Bytes(), want) {
		t.Fatalf("got %x want %x", buf.Bytes(), want)
	}
}
```

- [ ] **Step 2: Run to confirm fail**

```bash
cd 07_network/servers/websocket && go test -run 'TestReadMaskedTextFrame|TestWriteUnmaskedTextFrame' -v
```

Expected: FAIL with `undefined: ReadFrame`.

### Task 6.4: Implement frame codec

- [ ] **Step 1: Create `frame.go`**

```go
package main

import (
	"encoding/binary"
	"errors"
	"io"
)

type Opcode byte

const (
	OpCont  Opcode = 0x0
	OpText  Opcode = 0x1
	OpBin   Opcode = 0x2
	OpClose Opcode = 0x8
	OpPing  Opcode = 0x9
	OpPong  Opcode = 0xA
)

// ReadFrame reads a single WebSocket frame from r.
// Server-side: incoming client frames MUST be masked (RFC 6455 §5.1).
// L6: bit-packed frame header + XOR masking.
func ReadFrame(r io.Reader) (Opcode, []byte, error) {
	hdr := make([]byte, 2)
	if _, err := io.ReadFull(r, hdr); err != nil {
		return 0, nil, err
	}
	op := Opcode(hdr[0] & 0x0F)
	masked := hdr[1]&0x80 != 0
	plen := uint64(hdr[1] & 0x7F)
	switch plen {
	case 126:
		ext := make([]byte, 2)
		if _, err := io.ReadFull(r, ext); err != nil {
			return 0, nil, err
		}
		plen = uint64(binary.BigEndian.Uint16(ext))
	case 127:
		ext := make([]byte, 8)
		if _, err := io.ReadFull(r, ext); err != nil {
			return 0, nil, err
		}
		plen = binary.BigEndian.Uint64(ext)
	}
	if !masked {
		return 0, nil, errors.New("client frame must be masked")
	}
	mask := make([]byte, 4)
	if _, err := io.ReadFull(r, mask); err != nil {
		return 0, nil, err
	}
	payload := make([]byte, plen)
	if _, err := io.ReadFull(r, payload); err != nil {
		return 0, nil, err
	}
	for i := range payload {
		payload[i] ^= mask[i%4]
	}
	return op, payload, nil
}

// WriteFrame writes a single unmasked frame (server-side).
func WriteFrame(w io.Writer, op Opcode, payload []byte) error {
	var hdr []byte
	first := byte(0x80) | byte(op) // FIN=1
	switch {
	case len(payload) <= 125:
		hdr = []byte{first, byte(len(payload))}
	case len(payload) <= 0xFFFF:
		hdr = []byte{first, 126, 0, 0}
		binary.BigEndian.PutUint16(hdr[2:], uint16(len(payload)))
	default:
		hdr = []byte{first, 127, 0, 0, 0, 0, 0, 0, 0, 0}
		binary.BigEndian.PutUint64(hdr[2:], uint64(len(payload)))
	}
	if _, err := w.Write(hdr); err != nil {
		return err
	}
	if len(payload) > 0 {
		if _, err := w.Write(payload); err != nil {
			return err
		}
	}
	return nil
}
```

- [ ] **Step 2: Run frame tests**

```bash
cd 07_network/servers/websocket && go test -run 'TestReadMaskedTextFrame|TestWriteUnmaskedTextFrame' -v
```

Expected: PASS.

### Task 6.5: Hub tests

- [ ] **Step 1: Write failing `hub_test.go`**

```go
package main

import (
	"testing"
	"time"
)

func TestHubBroadcastsToSameRoom(t *testing.T) {
	h := NewHub()
	go h.Run()
	t.Cleanup(h.Stop)

	a := &fakeConn{out: make(chan []byte, 4)}
	b := &fakeConn{out: make(chan []byte, 4)}
	c := &fakeConn{out: make(chan []byte, 4)}

	h.Join("room1", a)
	h.Join("room1", b)
	h.Join("room2", c)

	h.Broadcast("room1", a, []byte("hello"))

	select {
	case msg := <-b.out:
		if string(msg) != "hello" {
			t.Fatalf("b got %q", msg)
		}
	case <-time.After(1 * time.Second):
		t.Fatal("b did not receive")
	}
	select {
	case <-c.out:
		t.Fatal("c (different room) must not receive")
	case <-time.After(100 * time.Millisecond):
	}
	select {
	case <-a.out:
		t.Fatal("sender a must not receive its own message")
	case <-time.After(100 * time.Millisecond):
	}
}

type fakeConn struct{ out chan []byte }

func (f *fakeConn) Send(b []byte)  { f.out <- b }
func (f *fakeConn) Close() error   { close(f.out); return nil }
```

- [ ] **Step 2: Run to confirm fail**

```bash
cd 07_network/servers/websocket && go test -run TestHubBroadcastsToSameRoom -v
```

Expected: FAIL with `undefined: NewHub`.

### Task 6.6: Implement Hub

- [ ] **Step 1: Create `hub.go`**

```go
package main

import "sync"

type Sender interface {
	Send([]byte)
}

type Hub struct {
	mu     sync.Mutex
	rooms  map[string]map[Sender]bool
	done   chan struct{}
}

func NewHub() *Hub {
	return &Hub{
		rooms: map[string]map[Sender]bool{},
		done:  make(chan struct{}),
	}
}

// L5: hub manages session membership per room.
func (h *Hub) Join(room string, c Sender) {
	h.mu.Lock()
	defer h.mu.Unlock()
	if h.rooms[room] == nil {
		h.rooms[room] = map[Sender]bool{}
	}
	h.rooms[room][c] = true
}

func (h *Hub) Leave(room string, c Sender) {
	h.mu.Lock()
	defer h.mu.Unlock()
	if h.rooms[room] != nil {
		delete(h.rooms[room], c)
	}
}

// L7: broadcast a chat message to peers in the same room.
func (h *Hub) Broadcast(room string, from Sender, msg []byte) {
	h.mu.Lock()
	peers := make([]Sender, 0, len(h.rooms[room]))
	for c := range h.rooms[room] {
		if c != from {
			peers = append(peers, c)
		}
	}
	h.mu.Unlock()
	for _, c := range peers {
		c.Send(msg)
	}
}

// Run/Stop are placeholders for future channel-based dispatch.
func (h *Hub) Run()  { <-h.done }
func (h *Hub) Stop() { close(h.done) }
```

- [ ] **Step 2: Run hub tests with race detector**

```bash
cd 07_network/servers/websocket && go test -race -run TestHubBroadcastsToSameRoom -v
```

Expected: PASS.

### Task 6.7: Server integration test

- [ ] **Step 1: Write failing `server_test.go`**

The server writes UNMASKED frames (server→client direction), so the test reads the response body with a small inline parser rather than `ReadFrame` (which requires the client→server masked format).

```go
package main

import (
	"bufio"
	"crypto/rand"
	"encoding/base64"
	"fmt"
	"io"
	"net"
	"strings"
	"testing"
	"time"
)

func TestServerBroadcastBetweenClients(t *testing.T) {
	ln, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	srv := NewServer(ln)
	go srv.Serve()
	t.Cleanup(func() { _ = ln.Close() })

	recv := dialAndUpgrade(t, ln.Addr().String(), "demo")
	defer recv.Close()
	send := dialAndUpgrade(t, ln.Addr().String(), "demo")
	defer send.Close()

	// give the receiver a moment to register with the hub
	time.Sleep(50 * time.Millisecond)

	if err := writeMaskedText(send, "hello"); err != nil {
		t.Fatal(err)
	}

	_ = recv.SetReadDeadline(time.Now().Add(2 * time.Second))
	hdr := make([]byte, 2)
	if _, err := io.ReadFull(recv, hdr); err != nil {
		t.Fatal(err)
	}
	if hdr[0] != 0x81 {
		t.Fatalf("opcode %x", hdr[0])
	}
	plen := int(hdr[1] & 0x7F)
	body := make([]byte, plen)
	if _, err := io.ReadFull(recv, body); err != nil {
		t.Fatal(err)
	}
	if string(body) != "hello" {
		t.Fatalf("got %q want %q", body, "hello")
	}
}

// dialAndUpgrade opens a TCP connection and performs the WebSocket handshake
// for path /ws?room=<room>.
func dialAndUpgrade(t *testing.T, addr, room string) net.Conn {
	t.Helper()
	c, err := net.Dial("tcp", addr)
	if err != nil {
		t.Fatal(err)
	}
	keyBytes := make([]byte, 16)
	_, _ = rand.Read(keyBytes)
	key := base64.StdEncoding.EncodeToString(keyBytes)
	req := fmt.Sprintf(
		"GET /ws?room=%s HTTP/1.1\r\nHost: x\r\nUpgrade: websocket\r\nConnection: Upgrade\r\n"+
			"Sec-WebSocket-Key: %s\r\nSec-WebSocket-Version: 13\r\n\r\n", room, key)
	if _, err := c.Write([]byte(req)); err != nil {
		t.Fatal(err)
	}
	r := bufio.NewReader(c)
	for {
		line, err := r.ReadString('\n')
		if err != nil {
			t.Fatal(err)
		}
		if strings.TrimRight(line, "\r\n") == "" {
			return c
		}
	}
}

// writeMaskedText writes a single text frame with masking (required from client).
func writeMaskedText(c net.Conn, msg string) error {
	if len(msg) > 125 {
		return fmt.Errorf("message too long for this helper (limit 125)")
	}
	mask := []byte{0xAA, 0xBB, 0xCC, 0xDD}
	masked := make([]byte, len(msg))
	for i := 0; i < len(msg); i++ {
		masked[i] = msg[i] ^ mask[i%4]
	}
	hdr := []byte{0x81, byte(0x80 | len(msg))}
	hdr = append(hdr, mask...)
	if _, err := c.Write(hdr); err != nil {
		return err
	}
	_, err := c.Write(masked)
	return err
}
```

- [ ] **Step 2: Run to confirm fail**

```bash
cd 07_network/servers/websocket && go test -run TestServerBroadcastBetweenClients -v
```

Expected: FAIL with `undefined: NewServer`.

### Task 6.8: Implement Server and connection wrapper

- [ ] **Step 1: Create `server.go`**

```go
package main

import (
	"bufio"
	"errors"
	"log/slog"
	"net"
	"sync"
)

type Server struct {
	ln  net.Listener
	hub *Hub
	log *slog.Logger
}

func NewServer(ln net.Listener) *Server {
	h := NewHub()
	go h.Run()
	return &Server{ln: ln, hub: h, log: slog.Default()}
}

func (s *Server) Serve() error {
	for {
		conn, err := s.ln.Accept()
		if err != nil {
			if errors.Is(err, net.ErrClosed) {
				return nil
			}
			return err
		}
		go s.handle(conn)
	}
}

func (s *Server) handle(conn net.Conn) {
	defer conn.Close()
	r := bufio.NewReader(conn)
	room, err := Upgrade(conn, r)
	if err != nil {
		s.log.Info("upgrade_err", "err", err.Error())
		return
	}
	s.log.Info("L5: upgraded", "room", room)

	c := &wsConn{conn: conn, send: make(chan []byte, 16)}
	s.hub.Join(room, c)
	defer s.hub.Leave(room, c)

	// writer goroutine: drains send channel, applies backpressure.
	go func() {
		for msg := range c.send {
			_ = WriteFrame(conn, OpText, msg)
		}
	}()

	for {
		op, payload, err := ReadFrame(r)
		if err != nil {
			c.close()
			return
		}
		s.log.Info("L6: frame", "op", op, "len", len(payload))
		switch op {
		case OpText:
			s.log.Info("L7: broadcast", "room", room, "len", len(payload))
			s.hub.Broadcast(room, c, payload)
		case OpPing:
			_ = WriteFrame(conn, OpPong, payload)
		case OpClose:
			_ = WriteFrame(conn, OpClose, payload)
			c.close()
			return
		}
	}
}

type wsConn struct {
	conn   net.Conn
	send   chan []byte
	once   sync.Once
	closed bool
}

func (c *wsConn) Send(b []byte) {
	select {
	case c.send <- b:
	default:
		// drop on slow consumer (backpressure)
	}
}

func (c *wsConn) close() {
	c.once.Do(func() { close(c.send); c.closed = true })
}

func (c *wsConn) Close() error { c.close(); return nil }
```

- [ ] **Step 2: Run all websocket tests with race detector**

```bash
cd 07_network/servers/websocket && go test -race -v
```

Expected: all PASS.

### Task 6.9: cmd/sender and cmd/receiver

- [ ] **Step 1: Create `cmd/sender/main.go`**

```go
package main

import (
	"bufio"
	"crypto/rand"
	"encoding/base64"
	"flag"
	"fmt"
	"net"
	"os"
	"strings"
)

func main() {
	addr := flag.String("addr", "localhost:9005", "server address")
	room := flag.String("room", "demo", "room name")
	flag.Parse()

	c, err := net.Dial("tcp", *addr)
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	defer c.Close()
	if err := handshake(c, *room); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	fmt.Fprintln(os.Stderr, "connected, type messages and press enter:")
	sc := bufio.NewScanner(os.Stdin)
	for sc.Scan() {
		if err := writeMaskedText(c, sc.Text()); err != nil {
			fmt.Fprintln(os.Stderr, err)
			return
		}
	}
}

func handshake(c net.Conn, room string) error {
	keyBytes := make([]byte, 16)
	_, _ = rand.Read(keyBytes)
	key := base64.StdEncoding.EncodeToString(keyBytes)
	req := fmt.Sprintf(
		"GET /ws?room=%s HTTP/1.1\r\nHost: x\r\nUpgrade: websocket\r\nConnection: Upgrade\r\n"+
			"Sec-WebSocket-Key: %s\r\nSec-WebSocket-Version: 13\r\n\r\n", room, key)
	if _, err := c.Write([]byte(req)); err != nil {
		return err
	}
	r := bufio.NewReader(c)
	for {
		line, err := r.ReadString('\n')
		if err != nil {
			return err
		}
		if strings.TrimRight(line, "\r\n") == "" {
			return nil
		}
	}
}

func writeMaskedText(c net.Conn, msg string) error {
	mask := []byte{0xAA, 0xBB, 0xCC, 0xDD}
	masked := make([]byte, len(msg))
	for i := 0; i < len(msg); i++ {
		masked[i] = msg[i] ^ mask[i%4]
	}
	if len(msg) > 125 {
		return fmt.Errorf("message too long for this demo (limit 125)")
	}
	hdr := []byte{0x81, byte(0x80 | len(msg))}
	hdr = append(hdr, mask...)
	if _, err := c.Write(hdr); err != nil {
		return err
	}
	_, err := c.Write(masked)
	return err
}
```

- [ ] **Step 2: Create `cmd/receiver/main.go`**

```go
package main

import (
	"bufio"
	"crypto/rand"
	"encoding/base64"
	"encoding/binary"
	"flag"
	"fmt"
	"io"
	"net"
	"os"
	"strings"
)

func main() {
	addr := flag.String("addr", "localhost:9005", "server address")
	room := flag.String("room", "demo", "room name")
	flag.Parse()

	c, err := net.Dial("tcp", *addr)
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	defer c.Close()
	if err := handshake(c, *room); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
	fmt.Fprintln(os.Stderr, "connected, waiting for messages...")
	for {
		op, payload, err := readUnmaskedFrame(c)
		if err != nil {
			fmt.Fprintln(os.Stderr, err)
			return
		}
		if op == 0x1 {
			fmt.Println(string(payload))
		}
	}
}

func handshake(c net.Conn, room string) error {
	keyBytes := make([]byte, 16)
	_, _ = rand.Read(keyBytes)
	key := base64.StdEncoding.EncodeToString(keyBytes)
	req := fmt.Sprintf(
		"GET /ws?room=%s HTTP/1.1\r\nHost: x\r\nUpgrade: websocket\r\nConnection: Upgrade\r\n"+
			"Sec-WebSocket-Key: %s\r\nSec-WebSocket-Version: 13\r\n\r\n", room, key)
	if _, err := c.Write([]byte(req)); err != nil {
		return err
	}
	r := bufio.NewReader(c)
	for {
		line, err := r.ReadString('\n')
		if err != nil {
			return err
		}
		if strings.TrimRight(line, "\r\n") == "" {
			return nil
		}
	}
}

func readUnmaskedFrame(c net.Conn) (byte, []byte, error) {
	hdr := make([]byte, 2)
	if _, err := io.ReadFull(c, hdr); err != nil {
		return 0, nil, err
	}
	op := hdr[0] & 0x0F
	plen := uint64(hdr[1] & 0x7F)
	switch plen {
	case 126:
		ext := make([]byte, 2)
		if _, err := io.ReadFull(c, ext); err != nil {
			return 0, nil, err
		}
		plen = uint64(binary.BigEndian.Uint16(ext))
	case 127:
		ext := make([]byte, 8)
		if _, err := io.ReadFull(c, ext); err != nil {
			return 0, nil, err
		}
		plen = binary.BigEndian.Uint64(ext)
	}
	body := make([]byte, plen)
	if _, err := io.ReadFull(c, body); err != nil {
		return 0, nil, err
	}
	return op, body, nil
}
```

- [ ] **Step 3: Create server `main.go`**

```go
package main

import (
	"context"
	"flag"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	addr := flag.String("addr", ":9005", "listen address")
	flag.Parse()

	ln, err := net.Listen("tcp", *addr)
	if err != nil {
		slog.Error("listen", "err", err.Error())
		os.Exit(1)
	}
	slog.Info("listening", "addr", ln.Addr().String())

	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()

	srv := NewServer(ln)
	go func() {
		<-ctx.Done()
		_ = ln.Close()
	}()
	_ = srv.Serve()
}
```

- [ ] **Step 4: Create `Dockerfile`**

```dockerfile
FROM golang:1.26-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/websocket .

FROM gcr.io/distroless/static
COPY --from=build /out/websocket /websocket
EXPOSE 9005
ENTRYPOINT ["/websocket"]
```

- [ ] **Step 5: Verify builds**

```bash
cd 07_network/servers/websocket && go build ./... && go build ./cmd/sender && go build ./cmd/receiver
```

Expected: exit 0.

### Task 6.10: Wire and commit

- [ ] **Step 1: Append `use ./servers/websocket` to `go.work`**

- [ ] **Step 2: Append to `docker-compose.yml`**

```yaml
  websocket:
    build: ./servers/websocket
    ports: ["9005:9005"]
```

- [ ] **Step 3: Append to `Makefile`**

```makefile
.PHONY: demo-ws
demo-ws:
	@echo "run sender/receiver in separate terminals:"
	@echo "  go run ./servers/websocket/cmd/receiver -room demo"
	@echo "  go run ./servers/websocket/cmd/sender   -room demo"
```

- [ ] **Step 4: Commit**

```bash
git add 07_network/
git commit -m "network(websocket): implement WebSocket hub with sender/receiver clients"
```

---

## Phase 7: docs 01–03 (concepts, TCP, UDP)

**Files:**
- Create: `07_network/docs/01_concepts.md`
- Create: `07_network/docs/02_tcp_basics.md`
- Create: `07_network/docs/03_udp_basics.md`

### Task 7.1: Write `01_concepts.md`

- [ ] **Step 1: Create the file with these sections**
  1. **イントロ**: 「TCP/UDP の上に HTTP/DNS/独自/WS が乗っている」を1段落で
  2. **OSI 7層 / TCP/IP 4層 対応表**: Markdownテーブル。L1〜L7、TCP/IP 4 層対応列、本章での扱い列
  3. **L5/L6/L7 は TCP/IP では潰されている**: 「TCP/IP 4 層では Application に束ねられているが、実際のプロトコルにはこの 3 層の役割が分かれて入っている」を明示
  4. **本章サーバーの位置づけ**: 6 サーバーがどの層のどの仕事をしているかを表で
  5. **観察ツール早見表**: tcpdump/ss/lsof/nc/curl/dig/xxd を 1 行ずつ
  6. **まとめ / 関連 doc / この先の話題**

- [ ] **Step 2: Verify with `wc -l` (~150–300 lines target)**

```bash
wc -l 07_network/docs/01_concepts.md
```

### Task 7.2: Write `02_tcp_basics.md`

- [ ] **Step 1: Create the file with these sections**
  1. **TCP の正体**: byte ストリーム / 接続志向 / 順序保証 / 再送（4 行で）
  2. **3-way handshake / FIN・RST**: ASCII シーケンス図
  3. **`servers/tcp-echo/` コードウォークスルー**: `server.go:NewServer`、`server.go:Serve`、`server.go:handle` の各行リンク (`servers/tcp-echo/server.go:14` 形式)
  4. **`Read` の戻り値が送信 byte 数と一致しない実験**: `make demo-tcp` で長文送信 → ログで複数 read 分割を観察する手順
  5. **観察コマンド**: `ss -tnl` / `tcpdump -i lo0 'tcp port 9001'` / `lsof -i :9001`
  6. **落とし穴**: 部分読み / `Close` の half-close / goroutine リーク / `SO_REUSEADDR`
  7. **L4 のみであることを明示**: 「素の TCP echo は L4 だけの世界で、L5/L6/L7 を持たない。後続章 (04/06/07) で TCP の上にこれらが乗ってくる対比を示す」を 1 段落
  8. **まとめ / 関連 doc / この先の話題**

- [ ] **Step 2: Verify line count (~300–400 lines target)**

### Task 7.3: Write `03_udp_basics.md`

- [ ] **Step 1: Create the file with these sections**
  1. **UDP の正体**: データグラム / 接続なし / 順序なし / ロス可 / 境界保持
  2. **TCP との対比表**
  3. **`servers/udp-echo/` コードウォークスルー**: `server.go` の行リンク
  4. **TCP と同じ長文を送る実験**: 「1 回の `ReadFrom` で全部届く」観察手順
  5. **MTU / フラグメンテーション / 65507 byte 上限**
  6. **落とし穴**: ロス検知不能 / 順序入れ替わり / 再送はアプリ責務
  7. **L4 のみであることを明示**: 「素の UDP echo は L4 だけ。後続章 (05) で UDP の上に DNS が乗ってくる対比を示す」を 1 段落
  8. **まとめ / 関連 doc / この先の話題**

- [ ] **Step 2: Verify line count (~200–300 lines target)**

### Task 7.4: Commit Phase 7

- [ ] **Step 1: Commit**

```bash
git add 07_network/docs/
git commit -m "network(docs): add 01_concepts, 02_tcp_basics, 03_udp_basics"
```

---

## Phase 8: docs 04–06 (HTTP, DNS, custom protocol)

**Files:**
- Create: `07_network/docs/04_http_on_tcp.md`
- Create: `07_network/docs/05_dns_on_udp.md`
- Create: `07_network/docs/06_custom_protocol.md`

### Task 8.1: Write `04_http_on_tcp.md`

- [ ] **Step 1: Create the file with these sections**
  1. **HTTP/1.1 の最小要素**: リクエストライン / ヘッダー / 空行 / 本文
  2. **`curl -v` の生 byte = サーバーが受け取る byte**: `curl -v http://localhost:9003/` 出力例
  3. **`servers/http/` コードウォークスルー**: `http.go:ParseRequest` / `server.go:handle` / `server.go:writeResponse` の行リンク
  4. **比較**: `net/http` で同等機能を書いた 30 行のスニペット
  5. **落とし穴**: `\r\n` 取り違え / Content-Length 無視 / ヘッダー大小無関心 / HTTP/0.9 との違い
  6. **L5/L6/L7 マッピング表**: 仕様書の表をそのまま転載、各層対応のコード行リンク（`servers/http/http.go:30` のように行番号付き）
  7. **この先**: keep-alive / chunked / HTTPS / HTTP/2 / HTTP/3 を 1 行ずつ
  8. **まとめ / 関連 doc**

- [ ] **Step 2: Verify line count (~400–500 lines target)**

### Task 8.2: Write `05_dns_on_udp.md`

- [ ] **Step 1: Create the file with these sections**
  1. **DNS メッセージフォーマット**: 12 byte ヘッダ / Question / Answer / Authority / Additional セクション
  2. **QNAME のラベル長プレフィックスと名前圧縮（ポインタ）**: ASCII 図 + 実際のヘックスダンプ例
  3. **`servers/dns/` コードウォークスルー**: `wire.go:ParseHeader` / `wire.go:DecodeQNAME` / `wire.go:EncodeAnswer` / `server.go:handle` の行リンク
  4. **実験**: `dig @localhost -p 5353 example.local +short` / `tcpdump -X -i lo0 'udp port 5353'` の出力例
  5. **対応範囲を明示**: A/IN 固定 / 再帰なし / 名前圧縮は書き込み側のみ
  6. **落とし穴**: バイトオーダー / TTL / TXID マッチ / UDP サイズ上限と TCP fallback
  7. **L5/L6/L7 マッピング表**: コード行リンク付き
  8. **この先**: 再帰解決 / TCP fallback / DNSSEC を 1 行ずつ
  9. **まとめ / 関連 doc**

- [ ] **Step 2: Verify line count (~400–500 lines target)**

### Task 8.3: Write `06_custom_protocol.md`

- [ ] **Step 1: Create the file with these sections**
  1. **「TCP 上で何故フレーミングが必要か」**: 02 章の `Read` 実験への回答として導入
  2. **長さプレフィックス / 区切り文字 / 固定長 の比較表**
  3. **フレームフォーマット図** (`Len(4) | Cmd(1) | Payload`)
  4. **`servers/lpp/` コードウォークスルー**: `frame.go:WriteFrame` / `frame.go:ReadFrame` / `server.go:dispatch` / `cmd/client/main.go:writeFrame` の行リンク
  5. **`io.ReadFull` を使う理由**: 短い読みでループしない罠
  6. **落とし穴**: エンディアン / サイズ上限と DoS 耐性 / 未知 Cmd の扱い / バージョニング
  7. **L5/L6/L7 マッピング表**: L5 を意図的に持たない設計の考察
  8. **まとめ / 関連 doc / この先の話題**

- [ ] **Step 2: Verify line count (~300–400 lines target)**

### Task 8.4: Commit Phase 8

- [ ] **Step 1: Commit**

```bash
git add 07_network/docs/
git commit -m "network(docs): add 04_http_on_tcp, 05_dns_on_udp, 06_custom_protocol"
```

---

## Phase 9: docs 07–08 (WebSocket, observability)

**Files:**
- Create: `07_network/docs/07_websocket.md`
- Create: `07_network/docs/08_observability.md`

### Task 9.1: Write `07_websocket.md`

- [ ] **Step 1: Create the file with these sections**
  1. **WebSocket が必要だった経緯**: ポーリング → long-polling → WS（1 段落）
  2. **ハンドシェイク**: `Upgrade: websocket` ヘッダ / `Sec-WebSocket-Key` → `Sec-WebSocket-Accept` の計算手順（SHA-1 + magic UUID + base64）
  3. **フレームフォーマット図**: FIN / opcode / MASK / Payload len / extended / masking key / payload（ビット位置を明示）
  4. **マスキング仕様**: クライアント→サーバーは必須、サーバー→クライアントは禁止、理由（プロキシキャッシュ汚染対策）
  5. **`servers/websocket/` コードウォークスルー**: `handshake.go:AcceptKey` / `handshake.go:Upgrade` / `frame.go:ReadFrame` / `frame.go:WriteFrame` / `hub.go:Broadcast` / `server.go:handle` / `cmd/sender/main.go` / `cmd/receiver/main.go` の行リンク
  6. **broadcast hub パターンの構造**: channel + goroutine 分離の図
  7. **実験**: 2 receiver + 1 sender、`tcpdump -X 'tcp port 9005'` で「HTTP テキスト→バイナリフレーム」の切替を観察する手順
  8. **落とし穴**: マスキング忘れ / ping/pong / close handshake / バックプレッシャ
  9. **L5/L6/L7 マッピング表**: コード行リンク付き
  10. **この先**: バイナリフレーム / permessage-deflate / サブプロトコル / WebTransport
  11. **まとめ / 関連 doc**

- [ ] **Step 2: Verify line count (~500–600 lines target)**

### Task 9.2: Write `08_observability.md`

- [ ] **Step 1: Create the file with these sections**
  1. **ツール早見表**: 02〜07 で散らばっている観察コマンドを集約
  2. **`tcpdump` 基本**: `-i lo0` / `-X` / `-A` / フィルタ式（`tcp port 9001` など）
  3. **`ss -tnl` / `ss -unl`**: listen 状態の確認
  4. **`lsof -i :PORT`**: そのポートを開いているプロセス
  5. **`nc` / `curl -v` / `dig +short +trace` / `xxd`**: 各 1 段落
  6. **macOS と Linux の差分**: `tcpdump -i lo0` vs `-i lo`、`ss` が macOS には無いため `lsof -i` で代替
  7. **Docker 環境での観察**: `docker compose exec` でコンテナ内側、`docker run --net=host` の選択肢
  8. **まとめ / 関連 doc**

- [ ] **Step 2: Verify line count (~200–300 lines target)**

### Task 9.3: Commit Phase 9

- [ ] **Step 1: Commit**

```bash
git add 07_network/docs/
git commit -m "network(docs): add 07_websocket and 08_observability"
```

---

## Phase 10: Finalization (top-level README, smoke test, learning/ link)

**Files:**
- Modify: `07_network/README.md`
- Modify: `README.md` (top-level learning README, link to 07_network)
- Verify: `make test-race`, all `make demo-*`

### Task 10.1: Write the final `07_network/README.md`

- [ ] **Step 1: Replace placeholder with full README**

```markdown
# 07_network: ネットワーク学習プロジェクト

TCP/UDP の上に HTTP/DNS/独自プロトコル/WebSocket が「足し算」で乗っていることを Go の `net` パッケージで体感する章。第三者ライブラリは使わず stdlib のみ。

## 学習動線

1. [01: 概念とレイヤーの位置づけ](docs/01_concepts.md)
2. [02: TCP の正体](docs/02_tcp_basics.md)
3. [03: UDP の正体](docs/03_udp_basics.md)
4. [04: TCP の上の HTTP](docs/04_http_on_tcp.md)
5. [05: UDP の上の DNS](docs/05_dns_on_udp.md)
6. [06: TCP の上の自作プロトコル](docs/06_custom_protocol.md)
7. [07: WebSocket（HTTP で始まり TCP に居座る）](docs/07_websocket.md)
8. [08: レイヤーを観るツール集](docs/08_observability.md)

## クイックスタート

```bash
make up           # 6 サーバー起動
make demo-tcp     # 素の TCP echo
make demo-udp     # 素の UDP echo
make demo-http    # HTTP/1.1
make demo-dns     # DNS A クエリ
make demo-lpp     # 自作プロトコル
make demo-ws      # WebSocket（sender/receiver 起動手順）
make test-race    # race 検出付き全テスト
make down
```

## サーバー一覧

| サーバー | ポート | プロトコル | docs |
|---|---|---|---|
| tcp-echo  | 9001/tcp  | 素の TCP | [02](docs/02_tcp_basics.md) |
| udp-echo  | 9002/udp  | 素の UDP | [03](docs/03_udp_basics.md) |
| http      | 9003/tcp  | HTTP/1.1 自前実装 | [04](docs/04_http_on_tcp.md) |
| dns       | 5353/udp  | DNS A クエリ自前実装 | [05](docs/05_dns_on_udp.md) |
| lpp       | 9004/tcp  | 長さプレフィックス独自プロトコル | [06](docs/06_custom_protocol.md) |
| websocket | 9005/tcp  | WebSocket + broadcast hub | [07](docs/07_websocket.md) |

## 環境注意

- **macOS** の `tcpdump` は loopback が `lo0`。**Linux** は `lo`。
- DNS 用ポートは衝突回避のため `5353/udp`。`dig @localhost -p 5353` で叩く。
- WebSocket クライアント（`cmd/sender`, `cmd/receiver`）はホスト側で `go run` する想定。
```

- [ ] **Step 2: Update learning/ top-level `README.md`** to link 07_network

```bash
ls README.md  # ensure it exists at repo root
```

If the top-level `README.md` already lists chapters, append a line `- [07_network](07_network/README.md) — ネットワーク学習`. If it does not, this step is a no-op (skip).

### Task 10.2: Verify acceptance criteria

- [ ] **Step 1: All tests pass with race detector**

```bash
cd 07_network && go test -race ./servers/...
```

Expected: PASS for all 6 modules.

- [ ] **Step 2: Bring up the full stack**

```bash
cd 07_network && docker compose up -d --build
docker compose ps
```

Expected: 6 services healthy / running.

- [ ] **Step 3: Verify each demo target**

```bash
cd 07_network
make demo-tcp    # expect "hello" echoed
make demo-udp    # expect "hello" echoed
make demo-http   # expect HTTP/1.1 200 OK + "Hello, world!"
make demo-dns    # expect "10.0.0.1"
make demo-lpp    # expect "0x01 " (ping reply)
```

- [ ] **Step 4: Verify L5/L6/L7 logs are visible**

```bash
cd 07_network
curl -s http://localhost:9003/ >/dev/null
docker compose logs http | grep -E 'L5|L6|L7'
```

Expected: log lines containing `L5: parsed request`, `L7: route`.

```bash
dig @localhost -p 5353 example.local +short >/dev/null
docker compose logs dns | grep -E 'L5|L6|L7'
```

Expected: log lines containing `L5: parsed TXID`, `L6: decoded QNAME`, `L7: answering A record`.

```bash
go run ./servers/lpp/cmd/client PING >/dev/null
docker compose logs lpp | grep -E 'L6|L7'
```

Expected: log lines containing `L6: decoded frame`, `L7: dispatch`.

For `websocket`: run `cmd/receiver` and `cmd/sender` in two terminals against `localhost:9005`, then `docker compose logs websocket | grep -E 'L5|L6|L7'`.

- [ ] **Step 5: Tear down**

```bash
cd 07_network && docker compose down
```

### Task 10.3: Final commit

- [ ] **Step 1: Commit**

```bash
git add 07_network/ README.md
git commit -m "network(readme): finalize 07_network README with learning path and quickstart"
```
